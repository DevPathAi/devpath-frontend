# 세션 핸드오프 — devpath-frontend (React→Flutter 전환 프로토)

> 작성: 2026-06-14 · 브랜치: `docs/flutter-migration-proto-ui-spec` · 다음 세션/담당자용 인계.

## 1. 한 줄 상태
React(web/admin) 스캐폴드 → **Flutter/Dart 모노레포 전환 + 목 API 프로토**의 **전체 설계·플랜(P1~P7)이 작성되고 다단계 리뷰까지 완료**됨. **코드 구현은 아직 시작 전.** 다음 단계 = (a) 남은 횡단 이슈 반영 → (b) 구현 착수(P1부터).

## 2. 산출물 지도 (모두 `docs/superpowers/` + 루트)
- **스펙(설계 SSoT)**: `specs/2026-06-14-react-to-flutter-and-proto-ui-design.md` — D1~D6 결정, §9 디자인 결정(DD1~DD8), 상태매트릭스, 반응형, a11y.
- **디자인 시스템 SSoT**: `DESIGN.md`(루트) — 토큰(색·Pretendard/D2Coding·8pt·반응형·a11y·모션). 디자인 리뷰 요약: `specs/2026-06-14-design-review-summary.md`.
- **구현 플랜 11종**: `plans/2026-06-14-p1..p7`
  - P1 모노레포 골격 / P2 dp_core(순수 Dart) / P3 dp_design(M3 토큰)
  - P4a 셸·인증·라우팅 / P4b 온보딩·PATH SSE(DD8) / P4c 콘텐츠·Sandbox·Monaco(DD5) / P4d AI리뷰·KILL_SWITCH/Quota / P4e 멘토 SSE / P4f 대시보드·커뮤니티
  - P5 admin(역할가드·테이블·신고) / P6 mobile(StatefulShellRoute·drift·FCM스텁) / P7 landing(Jaspr SSG, standalone)
- **횡단 이슈 정리**: `2026-06-14-cross-review-issues.md` ← **다음 세션 필독**.
- **보류 추적**: `TODOS.md`(루트).

## 3. 리뷰 이력 (무엇이 어디까지 검증됨)
| 플랜 | 리뷰 | 상태 |
|---|---|---|
| P1 | `/plan-ceo-review`(HOLD SCOPE) + `/plan-eng-review`(CLEAR) | 정식 + F1~F8·E1·E2 반영 |
| 스펙/UI | `/plan-design-review` 5/10→8/10, DD1~DD8 | 반영 |
| P2·P3 | 문서리뷰 | freezed 3.x·골든CI·scheme.primary·SSE flush 반영 |
| P4a~f | 문서리뷰(+P4b·P4f ENG-REVIEW 마커) | MediaQuery·SSE정규화·family→plain·DD3아이콘 반영 |
| P5·P6·P7 | 문서리뷰 | Jaspr Document.lang·config분리·DD3·drift/Jaspr Context7 검증 |
> 라이브러리 사실은 Context7로 검증(freezed 3.x / riverpod 3.3 / drift / Jaspr 0.22 / melos 7 / go_router).

## 4. 다음 세션이 먼저 할 일 (우선순위)
`cross-review-issues.md` 기준:
1. **A1**: P4f·P5 끝(또는 P8)에 web/admin **배포 재도입 태스크** 추가(T-DEPLOY-REINTRO).
2. **A2/A3**: P1 `ci.yml`에 **골든 전용 job** + **landing 전용 job** 추가.
3. **B1**: dp_core `MockHttpAdapter`를 **경로패턴+쿼리 인지**로 보강(아니면 데모 seeded-id 한계 명시).
4. **C1**: 구현 착수 전 누적분 일괄 `/plan-eng-review` 1회(특히 P5/P6/P7).
5. 이후 **구현 착수**: P1 Task 1부터 `superpowers:executing-plans`로 순차. (P1→P2→P3→P4a…→P7)

## 5. 구현 시 절대조건(반복 — CLAUDE.md)
- **추측 금지**: 라이브러리 API는 `pubspec.lock` 확정 후 설치본/Context7로 재확인(§D 목록).
- **Test-First**: 각 플랜이 실패테스트→최소구현→통과로 설계됨. `melos run test`로 눈으로 확인.
- **문제 시 코드 분석 우선**: 땜질 금지.

## 6. 환경 메모
- Windows + PowerShell. **Bash 도구에 PowerShell here-string(`@'...'@`) 쓰지 말 것**(커밋 메시지 깨짐 — 이번 세션 1회 발생·정정). 멀티라인 커밋은 `git commit -m "subject" -m "body"`.
- 작업 브랜치 `docs/flutter-migration-proto-ui-spec`(main 미머지). `.superpowers/`·`.omc/`는 gitignore.
- 미해결 TODOS: `T-DEPLOY-REINTRO`·`T-GOLDEN-CI-EXCLUDE②`·`T-LANDING-CI`·`T-CI-FLUTTER-PIN`·`T-SSE-ERR-NORMALIZE`(잔여) + 신규 Mock 보강 후보.

## 7. 미해결 결정(있으면 다음 세션이 사용자에 확인)
- A1 배포를 P4/P5에 넣을지 별도 P8로 뺄지.
- B1 Mock을 보강할지(구현 비용) vs 데모를 seeded-id로 제한할지.
- 구현 순서: 플랜대로 P1→P7 순차 vs 일부 병렬(worktree).
