# P6 — mobile 셸 + 대표 3화면 (대시보드·알림·오프라인) (Implementation Plan)

> 🔶 **Eng Review 반영(2026-06-14, D4 / F3)** — 결정 요약: [`../specs/2026-06-14-eng-review-summary.md`](../specs/2026-06-14-eng-review-summary.md). 반영분: D4-a 재연결 자동동기화 최소구현(`watchContents` StreamProvider 배선 + `isOfflineProvider` off 전이 1케이스), D4-b `ConnectivityService` 인터페이스화(F3, `PushService` 패턴), D4-c OAuth/`flutter_secure_storage` 후속 Task 이관(거버넌스), 딥링크 path 매핑 단위테스트, Task 4 분할(4a 셸 컴파일 / 4b 통합은 Task 6).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `apps/mobile`(Flutter)에 하단 5탭 셸(StatefulShellRoute) + 대표 3화면을 TDD로 구현한다 — **대시보드**(홈), **알림센터(M-NTF-001, FCM 스텁)**, **오프라인(M-OFF-001, drift 캐시 + 재연결 자동동기화 최소구현)**. `devpath://` 딥링크 path 매핑 라우팅 포함. (인증/OAuth·전체 동기화는 후속 — 리스크 절.)

**Architecture:** P4 web과 동일 코어(Riverpod 3·go_router·`ApiConfig.useMock`, `dio`-free) + mobile 전용 3종: **StatefulShellRoute.indexedStack**(탭 상태보존), **drift**(오프라인 캐시 — 코드젠), **FCM 추상화**(`PushService` 인터페이스 + 스텁, 실 firebase_messaging은 후속). drift DB는 provider로 주입(앱=`drift_flutter`, 테스트=`NativeDatabase.memory()`).

**Tech Stack:** Flutter · flutter_riverpod 3.3 · go_router(StatefulShellRoute) · **drift + drift_flutter**(오프라인) · `PushService`(FCM 스텁) · dp_core·dp_design · flutter_test.

---

> **선행:** P1(모노레포; **`mobile/`→`apps/mobile/` 이전 완료**)·P2(dp_core)·P3(dp_design)·(공통 패턴은 P4a 참조). 
> **참조:** 스펙 §2(mobile 하단 5탭·drift·FCM·딥링크)·§4(대표 3화면)·§9.2(알림센터·오프라인 행). DESIGN.md(상태위젯). 샘플: `Flutter_GoRouter_StatefulShellRoute_탭_네비게이션`·`Flutter_Dio_Offline_Cache_Interceptor`·`Flutter_SharedPreferences_Riverpod_KeepAlive_영속성`. 신규 API는 **Context7 검증**(go_router StatefulShellRoute·drift table/DB/watch·`navigationShell.goBranch`).
> **게이트(스펙 VERDICT):** mobile은 P6 — 아키텍처 영향(StatefulShellRoute·drift·딥링크) → 구현 전 `/plan-eng-review` 권장.
> **YAGNI/추측 경계:** **FCM은 스텁**(실 firebase_messaging+Firebase 프로젝트 설정은 후속 — 토큰/수신만 추상화). 연결성 감지는 `isOfflineProvider` 토글로 단순화(실 `connectivity_plus`는 후속). 딥링크 OS 설정(AndroidManifest/Info.plist)은 가이드만(코드는 go_router 라우트가 처리).

## P6가 소비하는 API

- dp_core(P2): `ApiClient.get`, `ApiException`, `DashboardSummary`(P4f). 
- dp_design(P3): `DpTheme`·`context.dpColors`, `DpLoading`·`DpEmpty`·`DpError`·`DpOfflineBanner`·`DpMarkdown`, `DpIcons`, `DpSpacing`.
- 신규: `drift`·`drift_flutter`(캐시 DB), go_router `StatefulShellRoute`.

---

## File Structure (P6에서 생성/수정)

```
apps/mobile/
├─ pubspec.yaml                                       # (수정) deps — Task 1
├─ android/app/src/main/AndroidManifest.xml           # (가이드) devpath:// intent-filter — Task 4
├─ ios/Runner/Info.plist                               # (가이드) CFBundleURLTypes — Task 4
├─ lib/
│  ├─ main.dart                                        # (교체) — Task 6
│  └─ src/
│     ├─ app/{app,router}.dart                          # 루트·StatefulShellRoute — Task 4·6
│     ├─ providers/{api_providers,theme_provider}.dart  # — Task 1
│     ├─ data/
│     │  ├─ cache_database.dart                          # drift DB(+codegen) — Task 2
│     │  ├─ mobile_mock_fixtures.dart                    # 목 — Task 1
│     │  ├─ connectivity_service.dart                    # 연결성 추상+스텁(F3) — Task 1
│     │  └─ push_service.dart                            # FCM 추상+스텁 — Task 3
│     └─ features/
│        ├─ shell/presentation/mobile_shell.dart         # 5탭 — Task 4
│        ├─ dashboard/...                                # 홈 — Task 5
│        ├─ notifications/...                            # M-NTF-001 — Task 5
│        └─ offline/...                                  # M-OFF-001 — Task 3·6
```

> 패키지명은 `apps/mobile/pubspec.yaml`의 `name:`(기존 mobile 데모 이전 — 보통 `devpath_mobile` 또는 기존명). 본 플랜은 **`devpath_mobile`** 가정, 다르면 일괄 치환.

---

## Task 1: deps + providers + 목

**Files:**
- Modify: `apps/mobile/pubspec.yaml`
- Create: `apps/mobile/lib/src/providers/api_providers.dart`, `.../theme_provider.dart`, `.../data/mobile_mock_fixtures.dart`
- Delete: 카운터 데모(`lib/main.dart`·`test/widget_test.dart` — Task 6서 교체)

- [ ] **Step 1: deps**

`apps/mobile/pubspec.yaml`의 `dependencies:`/`dev_dependencies:`에:
```yaml
dependencies:
  flutter:
    sdk: flutter
  dp_core:
    path: ../../packages/dp_core
  dp_design:
    path: ../../packages/dp_design
  flutter_riverpod: ^3.3.0
  go_router: ^14.6.0
  drift: ^2.20.0
  drift_flutter: ^0.2.0        # driftDatabase() — 네이티브 오픈
  # firebase_messaging은 후속(실 FCM). 프로토는 PushService 스텁.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.13
```
Run: `melos bootstrap`

- [ ] **Step 2: providers + 목 픽스처**

Create `apps/mobile/lib/src/providers/api_providers.dart`(web과 동일 패턴):
```dart
import 'package:dp_core/dp_core.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../data/mobile_mock_fixtures.dart';

class AppConfig {
  const AppConfig({required this.baseUrl, required this.useMock});
  factory AppConfig.fromEnvironment() => const AppConfig(
        baseUrl: String.fromEnvironment('API_BASE_URL',
            defaultValue: 'https://mock.devpath.ai/api/v1'),
        useMock: bool.fromEnvironment('USE_MOCK', defaultValue: true),
      );
  final String baseUrl;
  final bool useMock;
}

final appConfigProvider = Provider<AppConfig>((ref) => AppConfig.fromEnvironment());

final apiClientProvider = Provider<ApiClient>((ref) {
  final config = ref.watch(appConfigProvider);
  final client = ApiClient.create(
      ApiConfig(baseUrl: config.baseUrl, useMock: config.useMock));
  if (config.useMock) {
    client.dio.httpClientAdapter = MockHttpAdapter(mobileMockFixtures);
  }
  return client;
});

/// 연결성 배너 상태(연결성 도메인). FCM `PushService`와 동일하게 **인터페이스 경계**로
/// 추상화 — 실 `connectivity_plus` 교체 시 `StubConnectivityService`만 갈아끼우고
/// 호출부(`isOfflineProvider` watch)는 불변. (D4-b / F3)
/// ⚠️ 명명 주의: 이 provider는 **연결성 배너**(banner=connectivity)이며,
/// drift 오프라인 **보관함**(library=cache, `offlineProvider`)과 다른 도메인이다. (#8)
final isOfflineProvider = NotifierProvider<OfflineFlag, bool>(OfflineFlag.new);

class OfflineFlag extends Notifier<bool> {
  @override
  bool build() => ref.watch(connectivityServiceProvider).isOffline;
  void toggle() => state = !state; // 데모 토글(실 구현은 connectivity 스트림 반영)
}
```

Create `apps/mobile/lib/src/data/connectivity_service.dart`(`PushService`와 동일한 추상 경계):
```dart
/// 연결성 추상화. 실 구현은 connectivity_plus(후속). 프로토는 스텁.
/// 🔻 교체 경계: 실 구현 시 `StubConnectivityService` → `ConnectivityPlusService`로만 교체.
abstract interface class ConnectivityService {
  bool get isOffline;
  Stream<bool> get onOfflineChanged; // true=offline (스텁은 빈 스트림)
}

class StubConnectivityService implements ConnectivityService {
  @override
  bool get isOffline => false;
  @override
  Stream<bool> get onOfflineChanged => const Stream.empty();
}
```

Append to `apps/mobile/lib/src/providers/api_providers.dart`:
```dart
import '../data/connectivity_service.dart';

final connectivityServiceProvider =
    Provider<ConnectivityService>((ref) => StubConnectivityService());
```

Create `apps/mobile/lib/src/data/mobile_mock_fixtures.dart`:
```dart
import 'package:dp_core/dp_core.dart';

final Map<String, MockFixture> mobileMockFixtures = {
  'GET /dashboard': (
    200,
    {'streakDays': 5, 'progressPercent': 48, 'nextTaskTitle': 'Stream 실습', 'badges': ['첫 경로']},
  ),
  'GET /notifications': (
    200,
    {
      'data': [
        {'id': 'n1', 'title': '경로가 생성됐어요', 'body': '12주 로드맵 확인', 'read': false},
        {'id': 'n2', 'title': '멘토 답변 도착', 'body': '비동기 질문에 답변', 'read': true},
      ],
    },
  ),
  'GET /contents/c1': (
    200,
    {'markdown': '# 비동기 기초\n\noffline 캐시로도 읽을 수 있어요.'},
  ),
};
```

Create `apps/mobile/lib/src/providers/theme_provider.dart`(web과 동일):
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class ThemeModeController extends Notifier<ThemeMode> {
  @override
  ThemeMode build() => ThemeMode.system;
  void toggle() => state = state == ThemeMode.dark ? ThemeMode.light : ThemeMode.dark;
}

final themeModeProvider =
    NotifierProvider<ThemeModeController, ThemeMode>(ThemeModeController.new);
```

- [ ] **Step 3: 커밋**
```bash
git add apps/mobile/pubspec.yaml apps/mobile/lib/src/providers apps/mobile/lib/src/data/mobile_mock_fixtures.dart pubspec.lock
git commit -m "build(mobile): riverpod·go_router·drift 의존성 + providers·목"
```

---

## Task 2: drift 오프라인 캐시 DB

> Context7 검증: `Table`(TextColumn) + `@DriftDatabase` + `part '*.g.dart'`, `insertOnConflictUpdate`, `select().watch()`. 테스트는 `NativeDatabase.memory()`.

**Files:**
- Create: `apps/mobile/lib/src/data/cache_database.dart`
- Test: `apps/mobile/test/data/cache_database_test.dart`

- [ ] **Step 1: 실패 테스트(인메모리 DB upsert/read)**

Create `apps/mobile/test/data/cache_database_test.dart`:
```dart
import 'package:drift/native.dart';
import 'package:devpath_mobile/src/data/cache_database.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  late AppDatabase db;
  setUp(() => db = AppDatabase(NativeDatabase.memory()));
  tearDown(() => db.close());

  test('upsert 후 all로 읽는다', () async {
    await db.upsertContent('c1', '# 캐시');
    await db.upsertContent('c1', '# 갱신'); // 동일 id 갱신
    final rows = await db.allContents();
    expect(rows, hasLength(1));
    expect(rows.first.markdown, '# 갱신');
  });
}
```

- [ ] **Step 2: 실패 확인** — Run: `cd apps/mobile && flutter test test/data/cache_database_test.dart ; cd ../..` → FAIL

- [ ] **Step 3: 구현 + codegen**

Create `apps/mobile/lib/src/data/cache_database.dart`:
```dart
import 'package:drift/drift.dart';
import 'package:drift_flutter/drift_flutter.dart';

part 'cache_database.g.dart';

/// 오프라인 캐시 콘텐츠.
class CachedContents extends Table {
  TextColumn get id => text()();
  TextColumn get markdown => text()();

  @override
  Set<Column> get primaryKey => {id};
}

@DriftDatabase(tables: [CachedContents])
class AppDatabase extends _$AppDatabase {
  AppDatabase(super.e);

  @override
  int get schemaVersion => 1;

  Future<void> upsertContent(String id, String markdown) =>
      into(cachedContents).insertOnConflictUpdate(
          CachedContentsCompanion.insert(id: id, markdown: markdown));

  Future<List<CachedContent>> allContents() => select(cachedContents).get();

  Stream<List<CachedContent>> watchContents() => select(cachedContents).watch();
}

/// 앱 실행용 — 네이티브 파일 DB(테스트는 NativeDatabase.memory 주입).
AppDatabase openAppDatabase() => AppDatabase(driftDatabase(name: 'devpath_cache'));
```

Run codegen:
```bash
cd apps/mobile && dart run build_runner build --delete-conflicting-outputs ; cd ../..
```
Expected: `cache_database.g.dart` 생성(커밋).

- [ ] **Step 4: 통과 확인** — Run: `cd apps/mobile && flutter test test/data/cache_database_test.dart ; cd ../..` → PASS

- [ ] **Step 5: DB provider**

Append to `apps/mobile/lib/src/providers/api_providers.dart`:
```dart
import '../data/cache_database.dart';

final appDatabaseProvider = Provider<AppDatabase>((ref) {
  final db = openAppDatabase();
  ref.onDispose(db.close);
  return db;
});
```

- [ ] **Step 6: 커밋**
```bash
git add apps/mobile/lib/src/data/cache_database.dart apps/mobile/lib/src/data/cache_database.g.dart apps/mobile/lib/src/providers/api_providers.dart apps/mobile/test/data/cache_database_test.dart
git commit -m "feat(mobile): drift 오프라인 캐시 DB(+ provider)"
```

---

## Task 3: FCM 스텁(PushService) + 오프라인 컨트롤러

**Files:**
- Create: `apps/mobile/lib/src/data/push_service.dart`, `apps/mobile/lib/src/features/offline/application/offline_controller.dart`, `.../offline/state/offline_state.dart`
- Test: `apps/mobile/test/features/offline/offline_controller_test.dart`

- [ ] **Step 1: 실패 테스트(콘텐츠 캐시 후 오프라인 읽기)**

Create `apps/mobile/test/features/offline/offline_controller_test.dart`:
```dart
import 'package:drift/native.dart';
import 'package:devpath_mobile/src/data/cache_database.dart';
import 'package:devpath_mobile/src/features/offline/application/offline_controller.dart';
import 'package:devpath_mobile/src/features/offline/state/offline_state.dart';
import 'package:devpath_mobile/src/providers/api_providers.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('캐시 저장 후 오프라인 목록에 나타난다', () async {
    final db = AppDatabase(NativeDatabase.memory());
    addTearDown(db.close);
    final c = ProviderContainer(overrides: [
      appDatabaseProvider.overrideWithValue(db),
    ]);
    addTearDown(c.dispose);

    await c.read(offlineProvider.notifier).cacheContent('c1', '# 오프라인');
    await c.read(offlineProvider.notifier).load();

    final s = c.read(offlineProvider);
    expect(s, isA<OfflineLoaded>());
    expect((s as OfflineLoaded).items.single.markdown, '# 오프라인');
  });
}
```

- [ ] **Step 2: 실패 확인** — Run: `cd apps/mobile && flutter test test/features/offline/offline_controller_test.dart ; cd ../..` → FAIL

- [ ] **Step 3: 구현 — `push_service.dart`(FCM 추상 + 스텁)**

Create `apps/mobile/lib/src/data/push_service.dart`:
```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

/// 푸시 추상화. 실 구현은 firebase_messaging(후속). 프로토는 스텁.
abstract interface class PushService {
  Future<String?> getToken();
  Stream<String> get incoming; // 새 알림 타이틀 스트림(스텁)
}

class StubPushService implements PushService {
  @override
  Future<String?> getToken() async => 'stub-fcm-token';
  @override
  Stream<String> get incoming => const Stream.empty();
}

final pushServiceProvider = Provider<PushService>((ref) => StubPushService());
```
> 실 FCM: `firebase_messaging`로 `FirebaseMessaging.instance.getToken()`·`onMessage` 스트림을 구현한 `FcmPushService`로 교체 + Firebase 프로젝트 설정(google-services.json/plist). 본 프로토는 스텁(설정 없이 동작).
> **죽은 코드 명시(#7)**: `pushServiceProvider`는 본 P6에서 화면 결선하지 않는다 — **스텁 계약 테스트만** 두고(아래), 실 알림 수신 배선(`incoming` 스트림→알림 화면 갱신)은 후속 인증/FCM Task. `watchContents`는 Task 3 Step 5d에서 `watchedContentsProvider`로 배선됨(더 이상 죽은 코드 아님).

Append to `apps/mobile/test/features/offline/offline_controller_test.dart`(또는 `test/data/push_service_test.dart`) — 스텁 계약:
```dart
  test('StubPushService 계약 — 토큰·빈 스트림', () async {
    final svc = StubPushService();
    expect(await svc.getToken(), 'stub-fcm-token');
    expect(await svc.incoming.isEmpty, isTrue);
  });
```

- [ ] **Step 4: 구현 — offline state·controller**

Create `apps/mobile/lib/src/features/offline/state/offline_state.dart`:
```dart
import 'package:devpath_mobile/src/data/cache_database.dart';

sealed class OfflineState {
  const OfflineState();
}
class OfflineLoading extends OfflineState { const OfflineLoading(); }
class OfflineLoaded extends OfflineState {
  const OfflineLoaded(this.items);
  final List<CachedContent> items;
}
```

Create `apps/mobile/lib/src/features/offline/application/offline_controller.dart`:
```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../providers/api_providers.dart';
import '../state/offline_state.dart';

class OfflineController extends Notifier<OfflineState> {
  @override
  OfflineState build() => const OfflineLoading();

  Future<void> cacheContent(String id, String markdown) =>
      ref.read(appDatabaseProvider).upsertContent(id, markdown);

  Future<void> load() async {
    final items = await ref.read(appDatabaseProvider).allContents();
    state = OfflineLoaded(items);
  }
}

final offlineProvider =
    NotifierProvider<OfflineController, OfflineState>(OfflineController.new);
```

- [ ] **Step 5: 통과 확인** — Run: `cd apps/mobile && flutter test test/features/offline/offline_controller_test.dart ; cd ../..` → PASS

- [ ] **Step 5b: 실패 테스트 — 재연결 자동동기화(D4-a, §9.2 PARTIAL="재연결 자동동기화")**

> drift `watchContents`(L240, 죽은 코드)를 **StreamProvider**로 배선해 캐시 변경을 실시간 반영하고, `isOfflineProvider`가 **offline→online 전이** 시 1케이스 재동기화(목록 재로드)함을 검증한다. (#2·#7) 전체 동기화(네트워크 재페치·머지)는 후속(리스크 절).

Append to `apps/mobile/test/features/offline/offline_controller_test.dart`:
```dart
  test('watchContents 스트림이 upsert를 방출한다', () async {
    final db = AppDatabase(NativeDatabase.memory());
    addTearDown(db.close);
    final c = ProviderContainer(overrides: [
      appDatabaseProvider.overrideWithValue(db),
    ]);
    addTearDown(c.dispose);

    final sub = c.listen(watchedContentsProvider, (_, __) {});
    addTearDown(sub.close);

    await db.upsertContent('c1', '# 스트림');
    await Future<void>.delayed(Duration.zero);
    final rows = c.read(watchedContentsProvider).value;
    expect(rows, isNotNull);
    expect(rows!.single.markdown, '# 스트림');
  });

  test('offline→online 전이 시 재동기화(load)된다', () async {
    final db = AppDatabase(NativeDatabase.memory());
    addTearDown(db.close);
    final c = ProviderContainer(overrides: [
      appDatabaseProvider.overrideWithValue(db),
    ]);
    addTearDown(c.dispose);

    await c.read(offlineProvider.notifier).cacheContent('c1', '# 재연결');
    // offline 진입 후 다시 online 전이 → 자동 재동기화 1케이스
    c.read(isOfflineProvider.notifier).toggle(); // → offline(true)
    c.read(isOfflineProvider.notifier).toggle(); // → online(false): 재동기화 트리거
    await Future<void>.delayed(Duration.zero);

    final s = c.read(offlineProvider);
    expect(s, isA<OfflineLoaded>());
    expect((s as OfflineLoaded).items.single.markdown, '# 재연결');
  });
```

- [ ] **Step 5c: 실패 확인** — Run: `cd apps/mobile && flutter test test/features/offline/offline_controller_test.dart ; cd ../..` → FAIL(`watchedContentsProvider` 미정의·재동기화 미배선)

- [ ] **Step 5d: 구현 — `watchContents` StreamProvider 배선 + 재연결 재동기화**

Append to `apps/mobile/lib/src/features/offline/application/offline_controller.dart`:
```dart
/// drift `watchContents` 배선(L240 소비처) — 캐시 변경 실시간 스트림.
final watchedContentsProvider = StreamProvider.autoDispose((ref) =>
    ref.watch(appDatabaseProvider).watchContents());
```

`OfflineController.build()`에 **offline→online 전이 구독**을 추가(재연결 자동동기화 최소구현):
```dart
  @override
  OfflineState build() {
    // 재연결(offline→online) 전이 시 1케이스 재동기화. (D4-a)
    ref.listen<bool>(isOfflineProvider, (prev, next) {
      if (prev == true && next == false) load();
    });
    return const OfflineLoading();
  }
```
> 최소구현 경계: 전이 시 **drift 캐시 재로드(load)**만 수행. 네트워크 재페치·충돌 머지는 후속(리스크 절). `watchedContentsProvider`는 향후 화면 실시간 갱신/자동 캐시(`Flutter_Dio_Offline_Cache_Interceptor`)의 소비 지점.

- [ ] **Step 5e: 통과 확인** — Run: `cd apps/mobile && flutter test test/features/offline/offline_controller_test.dart ; cd ../..` → PASS

- [ ] **Step 6: 커밋**
```bash
git add apps/mobile/lib/src/data/push_service.dart apps/mobile/lib/src/data/connectivity_service.dart apps/mobile/lib/src/features/offline apps/mobile/test/features/offline
git commit -m "feat(mobile): PushService·ConnectivityService 스텁 + 오프라인 캐시 컨트롤러(drift, 재연결 자동동기화 최소구현)"
```

---

## Task 4: 하단 5탭 셸(StatefulShellRoute) + 라우터 + 딥링크

> Context7 검증: `StatefulShellRoute.indexedStack(builder:(c,s,navigationShell)=>…, branches:[StatefulShellBranch(routes:[…])])` + `navigationShell.currentIndex`/`goBranch(i)`.
>
> 🔶 **TDD 분할(Eng Review #6)**: 셸/라우터를 화면 전에 두면 통합 탭 전환 테스트가 화면(Task 5·6) 구현 전까지 green이 안 된다(red→green이 Task 내 안 닫힘). 따라서 **Task 4a**=셸/라우터 **컴파일·구조 최소**(라우트 path→page 매핑 단위테스트로 red→green 자기완결) / **Task 4b**=통합 탭 전환 위젯테스트는 화면이 준비되는 **Task 6으로 이동**. 커밋도 4a는 Task 4에서, 통합 테스트 커밋은 Task 6에서.

**Files:**
- Create: `apps/mobile/lib/src/features/shell/presentation/mobile_shell.dart`, `apps/mobile/lib/src/app/router.dart`
- Guide: `AndroidManifest.xml`/`Info.plist` (devpath:// — 코드 변경 아님)
- Test(4a): `apps/mobile/test/app/router_paths_test.dart` (path→page 매핑, 자기완결)
- Test(4b→Task 6): `apps/mobile/test/features/shell/mobile_shell_test.dart` (통합 탭 전환)

### Task 4a — 셸/라우터 컴파일 + 딥링크 path 매핑(자기완결 red→green)

- [ ] **Step 1: 실패 테스트(딥링크 path→page 매핑)**

> 통합 탭 전환(화면 의존) 대신, **라우터 단독**으로 검증 가능한 path 매핑을 red→green으로 닫는다: `devpath://notifications`가 매핑되는 `/notifications` initialLocation이 알림 화면을, 기본 `/home`이 대시보드를 렌더하는지. (딥링크 path 테스트 #5 — L519~522 매핑이 테스트 전무였음). 콜드스타트(OS intent 수신)는 가이드로 남기고 **path→page 매핑만** 단위테스트.

Create `apps/mobile/test/app/router_paths_test.dart`:
```dart
import 'package:devpath_mobile/src/app/router.dart';
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

Future<void> _pumpAt(WidgetTester tester, String location) async {
  final router = buildMobileRouter(initialLocation: location);
  await tester.pumpWidget(ProviderScope(
    child: MaterialApp.router(theme: DpTheme.light(), routerConfig: router),
  ));
  await tester.pumpAndSettle();
}

void main() {
  testWidgets('기본 /home → 대시보드(홈)', (tester) async {
    await _pumpAt(tester, '/home');
    expect(find.text('홈'), findsWidgets);
  });

  testWidgets('devpath://notifications == /notifications → 알림 센터', (tester) async {
    // go_router는 URI path를 그대로 매핑 → /notifications가 알림 화면을 띄운다.
    await _pumpAt(tester, '/notifications');
    expect(find.text('알림 센터'), findsOneWidget);
  });
}
```
> `buildMobileRouter`는 테스트 주입을 위해 `initialLocation` 파라미터를 받도록 한다(기본 `/home`). 이 테스트는 알림 화면(Task 5)에 의존하므로, **path 매핑 검증의 자기완결을 위해 Task 4a를 Task 5 뒤 순서로 실행**하거나, 알림 화면 최소 골격(AppBar `'알림 센터'`)을 4a에서 함께 둔다(아래 Step 3 주). 셸 구조/컴파일 자체는 본 Task에서 닫힌다.

- [ ] **Step 2: 실패 확인** — Run: `cd apps/mobile && flutter test test/app/router_paths_test.dart ; cd ../..` → FAIL

- [ ] **Step 3: 구현 — `mobile_shell.dart`**

Create `apps/mobile/lib/src/features/shell/presentation/mobile_shell.dart`:
```dart
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

/// 하단 5탭(StatefulShellRoute가 navigationShell 주입).
class MobileShell extends StatelessWidget {
  const MobileShell({super.key, required this.navigationShell});
  final StatefulNavigationShell navigationShell;

  static const _tabs = [
    (icon: DpIcons.home, label: '홈'),
    (icon: DpIcons.path, label: '경로'),
    (icon: DpIcons.mentor, label: '멘토'),
    (icon: DpIcons.community, label: '커뮤니티'),
    (icon: DpIcons.notifications, label: '알림'),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: navigationShell,
      bottomNavigationBar: NavigationBar(
        selectedIndex: navigationShell.currentIndex,
        onDestinationSelected: (i) => navigationShell.goBranch(
          i,
          initialLocation: i == navigationShell.currentIndex,
        ),
        destinations: [
          for (final t in _tabs)
            NavigationDestination(icon: Icon(t.icon), label: t.label),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: 구현 — `router.dart`**

Create `apps/mobile/lib/src/app/router.dart`:
```dart
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

import '../features/dashboard/presentation/dashboard_page.dart';
import '../features/notifications/presentation/notifications_page.dart';
import '../features/offline/presentation/offline_page.dart';
import '../features/shell/presentation/mobile_shell.dart';

/// 단순 플레이스홀더(경로/멘토/커뮤니티 — 모바일 프로토 범위 밖, web에 대응).
class _Tab extends StatelessWidget {
  const _Tab(this.title, this.icon);
  final String title;
  final IconData icon;
  @override
  Widget build(BuildContext context) =>
      Scaffold(appBar: AppBar(title: Text(title)), body: DpEmpty(icon: icon, title: title, message: '모바일 후속'));
}

GoRouter buildMobileRouter({String initialLocation = '/home'}) => GoRouter(
      initialLocation: initialLocation,
      routes: [
        StatefulShellRoute.indexedStack(
          builder: (_, __, navigationShell) =>
              MobileShell(navigationShell: navigationShell),
          branches: [
            StatefulShellBranch(routes: [
              GoRoute(path: '/home', builder: (_, __) => const MobileDashboardPage()),
              GoRoute(path: '/offline', builder: (_, __) => const OfflinePage()),
            ]),
            StatefulShellBranch(routes: [
              GoRoute(path: '/path', builder: (_, __) => const _Tab('학습 경로', DpIcons.path)),
            ]),
            StatefulShellBranch(routes: [
              GoRoute(path: '/mentor', builder: (_, __) => const _Tab('AI 멘토', DpIcons.mentor)),
            ]),
            StatefulShellBranch(routes: [
              GoRoute(path: '/community', builder: (_, __) => const _Tab('커뮤니티', DpIcons.community)),
            ]),
            StatefulShellBranch(routes: [
              GoRoute(path: '/notifications', builder: (_, __) => const NotificationsPage()),
            ]),
          ],
        ),
      ],
    );
```
> **딥링크(`devpath://`)**: go_router는 경로(`/notifications` 등)를 그대로 처리. OS 등록만 추가 —
> - Android `AndroidManifest.xml`의 main activity에 `<intent-filter>` + `<data android:scheme="devpath"/>` (+ `flutter_deeplinking_enabled` 메타).
> - iOS `Info.plist`에 `CFBundleURLTypes` → `CFBundleURLSchemes: [devpath]`.
> 등록 후 `devpath://notifications`가 `/notifications`로 라우팅(go_router가 URI path 매핑). 코드는 위 라우트로 충분. **path→page 매핑은 Step 1 `router_paths_test.dart`로 단위테스트 검증**(콜드스타트 OS intent 수신은 가이드만).

- [ ] **Step 4b: 셸 위젯 구현(컴파일 최소)** — `mobile_shell.dart`·`router.dart`로 셸이 컴파일/렌더되는지 확인. 통합 탭 전환(`mobile_shell_test.dart`)은 **Task 6 Step 1b로 이동**(화면 준비 후 green).

- [ ] **Step 5: 통과 확인(4a)** — Run: `cd apps/mobile && flutter test test/app/router_paths_test.dart ; cd ../..` → PASS
> 주(분할 #6): 화면 의존 통합 테스트(`mobile_shell_test.dart`)는 본 Task에서 작성/실행하지 않는다 — **Task 6에서 작성→green**. 본 Task 4a는 path 매핑 단위테스트로 red→green 자기완결.

- [ ] **Step 6: 커밋(4a)**
```bash
git add apps/mobile/lib/src/features/shell apps/mobile/lib/src/app/router.dart apps/mobile/test/app/router_paths_test.dart
git commit -m "feat(mobile): 하단 5탭 셸(StatefulShellRoute) + 딥링크 path 매핑 테스트"
```

---

## Task 5: 홈(대시보드) + 알림센터(M-NTF-001)

**Files:**
- Create: `apps/mobile/lib/src/features/dashboard/presentation/dashboard_page.dart`(+ controller/state), `.../notifications/...`
- Test: `apps/mobile/test/features/notifications/notifications_test.dart`

- [ ] **Step 1: 실패 테스트(알림 목록)**

Create `apps/mobile/test/features/notifications/notifications_test.dart`:
```dart
import 'package:devpath_mobile/src/features/notifications/presentation/notifications_page.dart';
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('알림 목록 렌더(목)', (tester) async {
    await tester.pumpWidget(ProviderScope(
      child: MaterialApp(theme: DpTheme.light(), home: const NotificationsPage()),
    ));
    await tester.pumpAndSettle();
    expect(find.text('알림 센터'), findsOneWidget);
    expect(find.textContaining('경로가 생성'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패 확인** — Run: `cd apps/mobile && flutter test test/features/notifications/notifications_test.dart ; cd ../..` → FAIL

- [ ] **Step 3: 구현 — 대시보드(홈, dp_core DashboardSummary)**

Create `apps/mobile/lib/src/features/dashboard/presentation/dashboard_page.dart`:
```dart
import 'package:dp_core/dp_core.dart';
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:go_router/go_router.dart';

import '../../../providers/api_providers.dart';

final mobileDashProvider = FutureProvider.autoDispose<DashboardSummary>((ref) async {
  final json = await ref.read(apiClientProvider).get<Map<String, dynamic>>('/dashboard');
  return DashboardSummary.fromJson(json);
});

class MobileDashboardPage extends ConsumerWidget {
  const MobileDashboardPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final async = ref.watch(mobileDashProvider);
    final offline = ref.watch(isOfflineProvider);

    return Scaffold(
      appBar: AppBar(
        title: const Text('홈'),
        actions: [
          IconButton(
            icon: const Icon(DpIcons.offline),
            tooltip: '오프라인 토글(데모)',
            onPressed: () => ref.read(isOfflineProvider.notifier).toggle(),
          ),
        ],
      ),
      body: Column(children: [
        if (offline) const DpOfflineBanner(),
        Expanded(
          child: async.when(
            loading: () => const DpLoading(),
            error: (e, _) => DpError(message: '$e', onRetry: () => ref.invalidate(mobileDashProvider)),
            data: (d) => ListView(
              padding: const EdgeInsets.all(DpSpacing.lg),
              children: [
                Text('${d.streakDays}일 연속 🔥',
                    style: Theme.of(context).textTheme.headlineSmall),
                const SizedBox(height: DpSpacing.md),
                LinearProgressIndicator(value: d.progressPercent / 100),
                const SizedBox(height: DpSpacing.xs),
                Text('진행률 ${d.progressPercent}%'),
                const SizedBox(height: DpSpacing.xl),
                FilledButton.tonalIcon(
                  onPressed: () => context.go('/offline'),
                  icon: const Icon(DpIcons.downloadDone),
                  label: const Text('오프라인 보관함'),
                ),
              ],
            ),
          ),
        ),
      ]),
    );
  }
}
```
> `FutureProvider.autoDispose`로 간결 — Riverpod `AsyncValue.when`. 아이콘은 `DpIcons.offline`/`DpIcons.downloadDone`(Symbols, P3 추가) — DD3 준수.

- [ ] **Step 4: 구현 — 알림센터(M-NTF-001)**

Create `apps/mobile/lib/src/features/notifications/presentation/notifications_page.dart`:
```dart
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../../../providers/api_providers.dart';

final notificationsProvider = FutureProvider.autoDispose<List<Map<String, dynamic>>>((ref) async {
  final json = await ref.read(apiClientProvider).get<Map<String, dynamic>>('/notifications');
  return (json['data'] as List).cast<Map<String, dynamic>>();
});

class NotificationsPage extends ConsumerWidget {
  const NotificationsPage({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final async = ref.watch(notificationsProvider);
    return Scaffold(
      appBar: AppBar(title: const Text('알림 센터')),
      body: async.when(
        loading: () => const DpLoading(),
        error: (e, _) => DpError(message: '$e', onRetry: () => ref.invalidate(notificationsProvider)),
        data: (items) => items.isEmpty
            ? const DpEmpty(icon: DpIcons.notifications, title: '새 알림이 없어요')
            : ListView(
                children: [
                  for (final n in items)
                    ListTile(
                      leading: Icon(DpIcons.notifications,
                          color: (n['read'] as bool? ?? false)
                              ? context.dpColors.textSecondary
                              : context.dpColors.primaryText),
                      title: Text(n['title'] as String),
                      subtitle: Text(n['body'] as String),
                    ),
                ],
              ),
      ),
    );
  }
}
```

- [ ] **Step 5: 통과 확인** — Run: `cd apps/mobile && flutter test test/features/notifications/notifications_test.dart ; cd ../..` → PASS

- [ ] **Step 6: 커밋**
```bash
git add apps/mobile/lib/src/features/dashboard apps/mobile/lib/src/features/notifications apps/mobile/test/features/notifications
git commit -m "feat(mobile): 홈 대시보드 + 알림센터(M-NTF-001)"
```

---

## Task 6: 오프라인 화면(M-OFF-001) + 앱 루트 + main + 검증

**Files:**
- Create: `apps/mobile/lib/src/features/offline/presentation/offline_page.dart`, `apps/mobile/lib/src/app/app.dart`, `apps/mobile/lib/main.dart`, `apps/mobile/test/mobile_smoke_test.dart`
- Delete: 카운터 데모(`lib/main.dart` 교체, `test/widget_test.dart`)

- [ ] **Step 1: 실패 테스트(스모크 — 탭/오프라인)**

Create `apps/mobile/test/mobile_smoke_test.dart`:
```dart
import 'package:drift/native.dart';
import 'package:devpath_mobile/src/app/app.dart';
import 'package:devpath_mobile/src/data/cache_database.dart';
import 'package:devpath_mobile/src/features/offline/application/offline_controller.dart';
import 'package:devpath_mobile/src/providers/api_providers.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('앱 시작 → 홈, 알림 탭 전환', (tester) async {
    final db = AppDatabase(NativeDatabase.memory());
    addTearDown(db.close);
    await tester.pumpWidget(ProviderScope(
      overrides: [appDatabaseProvider.overrideWithValue(db)],
      child: const DevPathMobileApp(),
    ));
    await tester.pumpAndSettle();

    expect(find.text('홈'), findsWidgets);
    await tester.tap(find.text('알림'));
    await tester.pumpAndSettle();
    expect(find.text('알림 센터'), findsOneWidget);
  });
}
```

- [ ] **Step 1b: 통합 셸 테스트(Task 4b 이관 — 탭 전환)**

> Task 4에서 화면 의존으로 미뤘던 통합 탭 전환 위젯테스트를 화면이 준비된 여기서 작성→green. (분할 #6)

Create `apps/mobile/test/features/shell/mobile_shell_test.dart`:
```dart
import 'package:devpath_mobile/src/app/router.dart';
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('하단 5탭 셸 — 탭 전환', (tester) async {
    final router = buildMobileRouter();
    await tester.pumpWidget(ProviderScope(
      child: MaterialApp.router(theme: DpTheme.light(), routerConfig: router),
    ));
    await tester.pumpAndSettle();

    expect(find.byType(NavigationBar), findsOneWidget);
    expect(find.text('홈'), findsWidgets);

    await tester.tap(find.text('알림'));
    await tester.pumpAndSettle();
    expect(find.text('알림 센터'), findsOneWidget); // 알림 탭 AppBar
  });
}
```

- [ ] **Step 2: 실패 확인** — Run: `cd apps/mobile && flutter test test/mobile_smoke_test.dart test/features/shell/mobile_shell_test.dart ; cd ../..` → FAIL

- [ ] **Step 3: 구현 — 오프라인 화면**

Create `apps/mobile/lib/src/features/offline/presentation/offline_page.dart`:
```dart
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../application/offline_controller.dart';
import '../state/offline_state.dart';

class OfflinePage extends ConsumerStatefulWidget {
  const OfflinePage({super.key});
  @override
  ConsumerState<OfflinePage> createState() => _S();
}

class _S extends ConsumerState<OfflinePage> {
  @override
  void initState() {
    super.initState();
    WidgetsBinding.instance.addPostFrameCallback((_) async {
      // 데모 시드: 콘텐츠 1개 캐시 후 1회 로드(이 화면=오프라인 **보관함**=drift 캐시).
      // 이후 캐시 변경은 watchedContentsProvider(스트림)로, 재연결(offline→online)
      // 전이 시 자동 재동기화는 OfflineController.build()의 ref.listen으로 반영. (#2·#7)
      await ref.read(offlineProvider.notifier).cacheContent(
          'c1', '# 비동기 기초\n\noffline 캐시로도 읽을 수 있어요.');
      await ref.read(offlineProvider.notifier).load();
    });
  }

  @override
  Widget build(BuildContext context) {
    final s = ref.watch(offlineProvider);
    return Scaffold(
      appBar: AppBar(title: const Text('오프라인 보관함')),
      body: switch (s) {
        OfflineLoading() => const DpLoading(),
        OfflineLoaded(:final items) when items.isEmpty =>
          const DpEmpty(icon: DpIcons.offline, title: '저장된 콘텐츠가 없어요'),
        OfflineLoaded(:final items) => ListView(
            padding: const EdgeInsets.all(DpSpacing.lg),
            children: [
              const DpOfflineBanner(),
              const SizedBox(height: DpSpacing.md),
              for (final it in items) DpMarkdown(data: it.markdown),
            ],
          ),
      },
    );
  }
}
```

- [ ] **Step 4: 구현 — 앱 루트 + main**

Create `apps/mobile/lib/src/app/app.dart`:
```dart
import 'package:dp_design/dp_design.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../providers/theme_provider.dart';
import 'router.dart';

class DevPathMobileApp extends ConsumerWidget {
  const DevPathMobileApp({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return MaterialApp.router(
      title: 'DevPath',
      debugShowCheckedModeBanner: false,
      theme: DpTheme.light(),
      darkTheme: DpTheme.dark(),
      themeMode: ref.watch(themeModeProvider),
      routerConfig: buildMobileRouter(),
    );
  }
}
```

Replace `apps/mobile/lib/main.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'src/app/app.dart';

void main() => runApp(const ProviderScope(child: DevPathMobileApp()));
```
구 카운터 테스트 제거: `rm apps/mobile/test/widget_test.dart`(있으면).

- [ ] **Step 5: 통과 + 전체 검증**

Run:
```bash
cd apps/mobile && flutter test ; cd ../..
melos run analyze
melos run test
```
Expected: mobile 전 테스트 PASS, `devpath_mobile` 포함 analyze 이슈 없음, 전 멤버 test PASS(골든 제외). drift codegen 산출물 커밋 확인.

- [ ] **Step 6: 커밋**
```bash
git add apps/mobile/lib/src/features/offline/presentation apps/mobile/lib/src/app/app.dart apps/mobile/lib/main.dart apps/mobile/test/mobile_smoke_test.dart apps/mobile/test/features/shell/mobile_shell_test.dart
git commit -m "feat(mobile): 오프라인 화면(M-OFF-001, drift) + 앱 루트 + 스모크 + 통합 셸 테스트(4b)"
```

---

## 검증 기준 (Definition of Done)

- [ ] `melos run analyze`/`test` — `devpath_mobile` 전 테스트 PASS(drift codegen 포함)
- [ ] 하단 5탭(StatefulShellRoute) — 탭 상태보존, 전환 동작
- [ ] 홈: 대시보드(스트릭·진행률) + 오프라인 토글 배너 + 오프라인 보관함 진입
- [ ] 알림센터(M-NTF-001): 목 알림 목록(읽음/안읽음 구분)
- [ ] 오프라인(M-OFF-001): drift 캐시 저장 → 목록/마크다운 렌더 + `DpOfflineBanner`
- [ ] 재연결 자동동기화(최소): `watchContents` StreamProvider 배선 + offline→online 전이 시 캐시 재로드 1케이스(테스트). 전체 동기화(네트워크 재페치·머지)는 후속.
- [ ] 연결성 경계(F3): `ConnectivityService` 인터페이스 + `StubConnectivityService`(실 `connectivity_plus`는 교체만).
- [ ] 딥링크: `devpath://notifications` → `/notifications` path 매핑 단위테스트(`router_paths_test.dart`) + OS 등록 가이드. 콜드스타트 intent 수신은 가이드.

## 리스크 / 후속 (명시)

- **FCM 스텁**: `PushService`는 스텁(`stub-fcm-token`, 빈 스트림). 실 `firebase_messaging`(토큰·onMessage·백그라운드 핸들러) + Firebase 프로젝트 설정(google-services.json/GoogleService-Info.plist)은 후속. 알림 목록은 목 REST.
- **연결성 감지 — 인터페이스 경계만(F3, D4-b)**: `ConnectivityService` 추상 + `StubConnectivityService`(`isOfflineProvider`가 watch). 실 `connectivity_plus` 교체 시 스텁만 갈아끼우면 호출부 불변. 자동 캐시 폴백(드리프트 우선→네트워크)은 후속.
- **재연결 자동동기화 — 최소구현(D4-a, §9.2 PARTIAL)**: ✅ 반영 — `watchContents` StreamProvider 배선 + offline→online 전이 시 캐시 재로드(`OfflineController.build` 내 `ref.listen`) 1케이스. **전체 동기화**(서버 재페치·델타 머지·충돌 해결)는 후속.
- **캐시 채우기 데모**: 오프라인 보관함 진입 시 콘텐츠 1개를 시드 캐시(데모). 실제로는 온라인 열람 시 자동 캐시(`Flutter_Dio_Offline_Cache_Interceptor` 패턴) — 후속.
- **OAuth 콜백·토큰 저장 — P6 스코프 밖(D4-c, 거버넌스)**: 스펙 §8 OAuth 콜백 딥링크와 `flutter_secure_storage` 기반 TokenStore는 본 플랜에 **미포함**. 후속 인증 Task(`TODOS: T-MOBILE-AUTH`)로 이관. 경계: 인증 콜백 유력안(외부 브라우저 + `devpath://callback`)은 현 5탭 딥링크 라우트(`devpath://notifications` 등 path 매핑)와 **스킴 충돌 없음**(별도 callback host 경로) — §8 미확정이므로 라우트 확정은 후속. 본 P6는 비인증 프로토 범위.
- **명명 분리(#8)**: 두 "offline" 도메인을 구분한다 — (1) **연결성 배너**(`isOfflineProvider`/`ConnectivityService`, banner=connectivity)와 (2) **오프라인 보관함**(`offlineProvider`/drift, library=cache). 코드 주석으로 표기, 향후 리네이밍(`connectivityBannerProvider`/`offlineLibraryProvider`) 권장.
- **딥링크 OS 설정 미포함**: AndroidManifest/Info.plist 변경은 가이드만(실제 파일 편집은 구현 시). go_router 라우트는 준비됨.
- **아이콘**: ✅ 반영 — `DpIcons.offline`/`DpIcons.downloadDone`(Symbols, P3 추가)로 교체. DD3 준수.
- **드리프트 웹 미대상**: 본 캐시는 mobile 전용. web 캐시는 범위 밖.
- **eng-review**: StatefulShellRoute·drift·딥링크는 아키텍처 영향 → 구현 전 `/plan-eng-review` 권장.
