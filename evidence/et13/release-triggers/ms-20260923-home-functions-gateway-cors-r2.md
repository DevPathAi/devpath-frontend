# ms-20260923-home-functions-gateway-cors-r2

This non-runtime marker triggers the path-filtered ET13 diagnostic evidence workflow for the 2026-09-23 release-evidence rebuild (home Pages Functions in the sealed dist + gateway CORS header dedupe). The release contract requires candidate web images that differ from production, so the frontend is rebuilt from a new main commit without product changes; the ET13 raw review at that commit is what the protected baseline approval binds to.
